import { colors } from './colors'
import { fonts } from './fonts'
import { semanticTokens } from './semantic-tokens'
import { textStyles } from './text-styles'

export default { colors, fonts, textStyles, semanticTokens }
