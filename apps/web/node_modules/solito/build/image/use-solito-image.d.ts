import { ImageProps } from 'react-native';
import { SolitoImageProps } from './image.types';
export declare type UseSolitoImage = Pick<ImageProps, Extract<keyof ImageProps, keyof SolitoImageProps> | 'progressiveRenderingEnabled' | 'source' | 'accessible' | 'onLayout' | 'style'> & {
    onLoadingComplete?: (info: {
        height: number;
        width: number;
    }) => void;
};
export declare function useSolitoImage({ src, loader, width, height, quality, crossOrigin, referrerPolicy, alt, fill, onLoadingComplete, loading, priority, placeholder, blurDataURL, sizes, style, onLayout, unoptimized, ...props }: SolitoImageProps): UseSolitoImage;
