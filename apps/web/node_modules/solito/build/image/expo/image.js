import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { Image } from 'react-native';
import { useSolitoImage } from '../use-solito-image';
const SolitoImage = forwardRef(function Img(props, ref) {
    const { onLoadingComplete, ...imageProps } = useSolitoImage(props);
    return (_jsx(Image, { ...imageProps, onLoad: onLoadingComplete && ((e) => onLoadingComplete(e.nativeEvent.source)), ref: ref }, void 0));
});
export default SolitoImage;
//# sourceMappingURL=image.js.map