import { colors } from './colors'
import { semanticTokens } from './semantic-tokens'
import { textStyles } from './text-styles'

const tokens = { colors, textStyles, semanticTokens }

export default tokens
